import React from "react";

function NotFound() {
  return (
    <div
      style={{
        width: 250,
        backgroundColor: "#ededed",
        padding: "20px 30px",
        marginLeft: "auto",
        marginRight: "auto",
        marginTop: "70%",
        borderRadius: 10,
        textAlign: "center",
      }}
    >
      <h5>Unable to find your credentials !</h5>
    </div>
  );
}

export default NotFound;
